# Holiday Calendar — 基準.md

最終更新: 2026-08-08（Railwayへデプロイ、公開URL発行）
状態: 稼働中（公開済み）

開発ロードマップはアプリ内「開発ロードマップ」タブに表示（`app.py` の `render_roadmap_tab()`）。

---

## 概要

2026年・2027年の日本×オーストラリア(VIC州)祝日カレンダー。Streamlit製。
月表示／リスト表示の切替、国別（日本／オーストラリア）フィルターに対応。
ユーザー登録・ログインに対応し、ログインした本人のメモ・予定だけが見える（他ユーザーのデータは見えない）。

---

## フォルダ構成

```text
Holiday Calendar/
  app.py              Streamlitアプリ本体（ログイン画面＋カレンダータブ＋開発ロードマップタブ）
  requirements.txt    依存パッケージ（streamlit, holidays）
  users.json           ユーザー一覧（ユーザー名／パスワードのソルト＋ハッシュ）。初回登録時に自動生成、Git管理対象外
  user_data/            ユーザーごとのメモ・予定の保存先（例: user_data/satoko_events.json）。Git管理対象外
  基準.md
```

---

## 重要ファイル

| ファイル | 役割 |
|---|---|
| `基準.md` | このプロジェクトの正本 |
| `app.py` | エントリーポイント。ログイン/新規登録、祝日データ取得(`holidays`ライブラリ)、月表示/リスト表示、ロードマップタブ |
| `requirements.txt` | `pip install -r requirements.txt` で依存関係を導入 |

---

## 主な機能

- ユーザー登録・ログイン（ユーザー名＋パスワード）。パスワードは平文保存せず、PBKDF2-SHA256でソルト付きハッシュ化して `users.json` に保存
- ログインしたユーザーのメモ・予定は `user_data/<ユーザー名>_events.json` に個別保存され、他ユーザーからは見えない（AppTestでデータ分離を確認済み）
- サイドバーに「ログアウト」ボタン
- 2026年・2027年の切替表示
- 日本の祝日（`holidays.Japan`）、オーストラリアの祝日（`holidays.Australia(subdiv="VIC")` = 全国共通＋VIC州）
- 国別フィルター（チェックボックスでON/OFF）
- 月表示（全12ヶ月を一覧）／リスト表示（日付順の一覧表）の切替
- 個人メモ・予定の入力フォーム（サイドバー）。ログイン中のユーザーごとにローカル保存され、アプリを再起動しても内容が残る。公開・共有はされない
- 保存済みメモの一覧表示＋各行に「編集」「削除」ボタン（編集は該当行がテキスト入力欄に切り替わり、💾保存／取消で確定）
- 開発ロードマップタブ（このアプリ自体の開発計画を表示）

---

## 起動方法

```bash
cd "AI work space/Holiday Calendar"
python -m pip install -r requirements.txt
python -m streamlit run app.py
```

初回はアカウントが1件もないので、「新規登録」タブからユーザー名・パスワードを作成してから使う。

---

## デプロイ

- 本番URL: https://holiday-calendar-production.up.railway.app
- ホスティング: Railway（プロジェクト名 `holiday-calendar`、`satoko.digicert@gmail.com` アカウント配下、Hobbyプラン以上へのアップグレード後に作成）
- 起動コマンド: `Procfile` の `web: streamlit run app.py --server.port=$PORT --server.address=0.0.0.0 --server.headless=true`
- デプロイ方法: `railway up` による手動デプロイ。GitHub連携による自動デプロイは未設定
- GitHubリポジトリ: https://github.com/SatokoBrook/holiday-calendar （public。`users.json`・`user_data/` は `.gitignore` でリポジトリに含めていない）
- 注意: Railwayのファイルシステムはコンテナ再作成時にリセットされる可能性があり、`users.json`（登録ユーザー）・`user_data/`（個人メモ）は再デプロイ・再起動で消える場合がある。実験目的として許容済み（本人合意）
- Streamlit Community Cloudへのデプロイは検討したが、GitHub OAuthのブラウザ手動ログインが必要でエージェント側から完結できないため、Railwayに切り替えて公開した

---

## 現フェーズ

Phase 0〜4 完了（土台・日豪祝日・表示切替・ロードマップタブ・個人メモCRUD・ログイン機能）。次候補: 重複祝日の強調表示、.icsエクスポート、他州対応、Webデプロイ。詳細はアプリ内「開発ロードマップ」タブ参照。

---

## 次のアクション

- [ ] 重複祝日（同日に日豪両方の祝日がある日）を目立たせる表示
- [ ] .icsエクスポート機能
- [ ] VIC以外の豪州州にも対応
- [x] Webデプロイ（Railwayで公開済み、2026-08-08）

---

## 作業ログタグ

`~/.claude/作業ログ/` で `Holiday Calendar` を検索
